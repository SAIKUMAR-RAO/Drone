kind: pipeline
name: default

steps:
- name: ping
  image: redis
  commands:
  - redis-cli -h cache ping

steps:
- name: docker  
  image: sai06/caching:v1
  settings:
    username: ${DOCKER_USER}
    password: ${DOCKER_PASSWORD}
    tags: latest
